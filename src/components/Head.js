import styled from "styled-components"

const Header = styled.header`
    background-color: red;
    width: 100%;
`;
const Head = () => {

    return (
        <Header>
            Head
        </Header>
    )
}

export default Head